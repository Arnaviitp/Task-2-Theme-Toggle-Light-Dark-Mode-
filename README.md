# Task-2-Theme-Toggle-Light-Dark-Mode

Approach:

Used CSS variables to manage theme colors.

Added toggle button to switch between light & dark.

Implemented smooth transitions, animations, gradient backgrounds, glassmorphism, parallax for a stylish feel.

Used localStorage to persist theme.
